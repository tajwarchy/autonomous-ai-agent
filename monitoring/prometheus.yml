global:
  scrape_interval: 10s        # pull metrics every 10 seconds
  evaluation_interval: 10s

scrape_configs:
  - job_name: "autonomous-agent"
    static_configs:
      - targets: ["agent:8000"]   # Docker service name + port
    metrics_path: "/metrics"